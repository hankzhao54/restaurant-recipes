# restaurant-recipes 维护指南（Mac）

一个 React + Vite + Supabase 的餐厅菜谱 App。前端托管在 Vercel（push 到 GitHub 自动部署），数据在 Supabase。

---

## 一、第一次在这台 Mac 上（只做一次）

1. **装 Node.js**（如果还没装）：去 https://nodejs.org 下载 LTS 版，或终端里 `brew install node`。
2. **双击项目里的 `setup.command`**：它会自动删掉旧依赖、重新 `npm install`、测试构建。
   - 从别的电脑复制过来的 `node_modules` **不能直接用**，必须重装 —— 脚本已经帮你处理。
   - 如果双击提示"无法打开，因为来自身份不明的开发者"：右键点它 → 选「打开」→ 确认。

跑完看到 `✅ 构建通过` 就说明环境好了。

---

## 二、日常开发

在项目文件夹打开终端（Finder 里右键文件夹 → 服务 → 新建位于文件夹位置的终端标签页），然后：

```bash
npm run dev
```

终端会显示一个网址（一般是 `http://localhost:5173`），浏览器打开就能实时预览。改代码保存后页面自动刷新。按 `Ctrl+C` 停止。

**主要文件：**
- `index.html` — 页面外壳、字体、全局样式
- `src/App.jsx` — 几乎所有界面和逻辑都在这里
- `src/main.jsx` — 入口
- `supabase-*.sql` — 数据库建表 / 种子数据脚本

---

## 三、改完怎么上线

Vercel 已经连了这个 GitHub 仓库，**你只要 push 到 `main`，它就自动部署**：

```bash
git add -A
git commit -m "简短说明改了什么"
git push
```

一两分钟后线上就更新了。想确认可以去 Vercel 后台看部署状态。

> 建议：大改动前先 `npm run dev` 本地确认没报错、功能正常，再 push。

---

## 四、环境变量（Supabase 密钥）

- 密钥放在 `.env.local`（已存在，**不会**被 git 提交，见 `.gitignore`）。
- 变量名必须以 `VITE_` 开头才能被前端读到：`VITE_SUPABASE_URL`、`VITE_SUPABASE_ANON_KEY`。
- 换新电脑 / 密钥丢了：照 `.env.example` 复制成 `.env.local`，去 Supabase 后台 Settings → API 复制真实值填进去。
- **Vercel 上也要单独配一份**同名环境变量（Project → Settings → Environment Variables），否则线上读不到。

---

## 五、常用命令速查

| 命令 | 作用 |
|---|---|
| `npm run dev` | 本地开发预览（热更新） |
| `npm run build` | 打包生产版本到 `dist/` |
| `npm run preview` | 本地预览打包后的结果 |
| `npm install` | 重装 / 更新依赖 |
| `git status` | 看改了哪些文件 |
| `git push` | 推送并触发 Vercel 部署 |

---

## 六、遇到问题

- **`vite: command not found` / `Permission denied`** → 依赖没装好或来自别的平台。删掉 `node_modules` 重新双击 `setup.command`。
- **页面白屏、控制台报 Supabase 错** → 检查 `.env.local` 密钥是否正确。
- **push 后线上没更新** → 去 Vercel 后台看部署是否失败，或环境变量是否配了。
- 其它报错，直接把终端里的红字发给 Claude。
